name: Publish GHCR image

on:
  push:
    branches:
      - main
    paths:
      - "**"
      - ".github/workflows/publish-ghcr.yml"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    name: Build and push image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set image name
        id: image
        shell: bash
        run: echo "name=${REGISTRY}/${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.image.outputs.name }}
          tags: |
            type=ref,event=branch
            type=sha,prefix=sha-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            APP_VERSION=${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Write deployment summary
        shell: bash
        run: |
          {
            echo "### Published image tags"
            echo '```text'
            echo "${{ steps.meta.outputs.tags }}"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

  deploy-sealos:
    name: Deploy to Sealos
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Setup kubeconfig
        run: |
          mkdir -p "$HOME/.kube"
          echo "${{ secrets.SEALOS_KUBECONFIG }}" > "$HOME/.kube/config"

      - name: Update image
        run: |
          curl -LO "https://dl.k8s.io/release/v1.30.0/bin/linux/amd64/kubectl"
          chmod +x kubectl
          ./kubectl set image statefulset/slicer-labeler \
            slicer-labeler=${{ env.REGISTRY }}/${{ github.repository }}:sha-${{ github.sha }}

      - name: Wait for rollout
        run: |
          ./kubectl rollout status statefulset/slicer-labeler --timeout=120s
